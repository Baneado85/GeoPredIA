COLUMN TABLE "GEOPREDIA_ZONES" (
    "ZONE_ID" NVARCHAR(36) NOT NULL,
    "NAME" NVARCHAR(100) NOT NULL,
    "REGION" NVARCHAR(100),
    "LATITUDE" DECIMAL(9, 6),
    "LONGITUDE" DECIMAL(9, 6),
    "CREATED_AT" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("ZONE_ID")
);
